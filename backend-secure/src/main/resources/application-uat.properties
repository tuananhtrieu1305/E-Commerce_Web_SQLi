# ============================================================
# Backend-Secure — UAT Profile (Docker)
# DB user: app_secure (Least Privilege: SELECT/INSERT/UPDATE only)
# ============================================================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/e_commerce_web}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:app_secure}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:SecurePass@2024}
