# ============================================================
# Backend-Vulnerable — UAT Profile (Docker)
# DB user: root (full privileges) — CỐ TÌNH không bảo mật
# ============================================================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/e_commerce_web}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:123456}
